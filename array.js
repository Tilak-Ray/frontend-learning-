let myArr = [1, 2, 3, 4, 5, 6, 7];

// console.log(myArr);

// console.log(myArr.push(8));           // it add the given element at the end of the array
// console.log(myArr);

// console.log(myArr.pop());           // it removes the last element from the array
// console.log(myArr);

// console.log(myArr.unshift(2));     // it add the given element in the biggning of the array
// console.log(myArr);

// console.log(myArr.shift());   // it remove the first element from the array
// console.log(myArr);

// console.log(myArr.includes(4));         //it finds if this array contain this element or not

// console.log(myArr.indexOf(3));  //shows the index of the given element in array

// console.log(myArr.join()); // it just join every element as string

// console.log(myArr.slice(3, 5));     // it just virtually cut and show the part of the array

// console.log(myArr.splice(3, 5));    // it actually remove that given part from the array and left with rest element

// console.log(myArr);


